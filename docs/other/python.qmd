---
title: Jupyter Widgets and `ipython`
format: live-html
resources:
  - "images"
pyodide:
  packages:
    - matplotlib
    - ipywidgets
---

```{pyodide}
from IPython.display import Image
Image(filename='images/python-logo.png') 
```

```{pyodide}
from IPython.display import HTML
display(HTML("<h3>A Heading</h3>"))
display(HTML("<b>Bold text!</b>"))
```

```{pyodide}
import ipywidgets as widgets
widgets.IntSlider(
    value=7,
    min=0,
    max=10,
    step=1,
    description='Test:',
    disabled=False,
    continuous_update=False,
    orientation='horizontal',
    readout=True,
    readout_format='d'
)
```
