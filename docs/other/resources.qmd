---
format: live-html
title: "Loading Resources"
resources:
  - "data"
---

```{pyodide}
from os import listdir
listdir('data')
```

```{webr}
list.files("data")
mt <- read.csv("data/mtcars.csv")
mod <- glm(mpg ~ cyl, data = mt)
summary(mod)
plot(mod)
```
