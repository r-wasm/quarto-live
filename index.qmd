---
format: html
engine: markdown
filters:
  - webr_ojs_quarto.lua
---

```{webr}
bar <- list(x = c(1, 2, 3), y = c(4, 5, 6))
foo <- function (x) { 1 + sin(x); }
foo2 <- function (x) { list(a = x, b = x * x ) }
```

```{ojs}
foo(3)
foo([1,2,3])
foo2(5)
```
